# Нижние границы, а не точные версии: приложение живёт долго, и запрещать
# обновления безопасности ради воспроизводимости — плохой размен.
#
# Исключение — uvicorn. Он закреплён точно, и вот почему: в 0.49 упавшая
# инициализация приложения просто ставила флаг, а в 0.52 стала завершать
# процесс с кодом 3. Локально стояла 0.49, в сборку CI клал 0.52 — и на
# Windows программа не запускалась, а воспроизвести это на своей машине было
# невозможно. Выпускать надо ровно то, на чём прогоняются тесты.
fastapi>=0.115
uvicorn==0.52.0
jinja2>=3.1
python-multipart>=0.0.9
requests>=2.32
beautifulsoup4>=4.12
pypdf>=5.0
apscheduler>=3.10
pywebview>=5.0
