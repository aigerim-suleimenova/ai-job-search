# Lower bounds rather than exact versions: the app lives a long time, and blocking
# security updates for the sake of reproducibility is a bad trade.
#
# The exception is uvicorn. It is pinned exactly, and here is why: in 0.49 a failed
# application startup simply set a flag, while in 0.52 it began terminating the
# process with exit code 3. Locally 0.49 was installed while CI put 0.52 into the
# build — and on Windows the program would not start, which was impossible to
# reproduce on our own machine. Ship exactly what the tests ran against.
fastapi>=0.115
uvicorn==0.52.0
jinja2>=3.1
python-multipart>=0.0.9
requests>=2.32
beautifulsoup4>=4.12
pypdf>=5.0
apscheduler>=3.10
pywebview>=5.0
